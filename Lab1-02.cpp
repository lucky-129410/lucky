#define N 10010
#include<cstdio>
#include<iostream>

using namespace std;

int findSpecialInteger(int* arr, int arrSize){
    // 此处完善函数，返回指定元素

}

int main() {

    int n, arr[N];

    cin >> n;
    for(int i = 0; i < n; i++)
        cin >> arr[i];

    cout << findSpecialInteger(arr, n) << endl;

    // 时间复杂度：
    // 空间复杂度：

    return 0;
}